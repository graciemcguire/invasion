<h1>👽 hello were the aliens 👽</h1>
<div class="alien-index">

  <% @aliens.each do |alien| %>
  <a href=<%= "/aliens/" + alien.id.to_s %>  class="alien-card">
    <img class="alien-image" src='<%= alien.image %>', alt= <%= alien.name %> />
    <p><%= alien.name %></p>
  </a>
  <% end %>

</div>
